import React from "react";

const createGroup = (props) => {
    return(
        <div>
            <p>
                Create Group
            </p>
        </div>
    );
};

export default createGroup;